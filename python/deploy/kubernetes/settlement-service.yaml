apiVersion: apps/v1
kind: Deployment
metadata:
  name: settlement-service
  namespace: payflow
spec:
  replicas: 1
  selector:
    matchLabels:
      app: settlement-service
  template:
    metadata:
      labels:
        app: settlement-service
    spec:
      containers:
        - name: settlement-service
          image: payflow/settlement-service:latest
          ports:
            - containerPort: 8084
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: payflow-config
                  key: PORT_SETTLEMENT
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: payflow-secret
                  key: DATABASE_URL
          readinessProbe:
            httpGet:
              path: /health
              port: 8084
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8084
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: settlement-service
  namespace: payflow
spec:
  selector:
    app: settlement-service
  ports:
    - port: 8084
      targetPort: 8084
